% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ridge.R
\name{plot_ridge}
\alias{plot_ridge}
\title{Ridge plot for microbiome composition across samples}
\usage{
plot_ridge(
  data,
  level = "Family",
  color_by = "Phylum",
  top_n = 12,
  group_by = NULL,
  palette = NULL,
  scale = 2,
  alpha = 0.8
)
}
\arguments{
\item{data}{A list with elements \code{otu_table}, \code{tax_table}, and \code{sample_data},
as in \link{demo_data}.}

\item{level}{Taxonomic level to display. Default is \code{"Family"}.}

\item{color_by}{Taxonomic level for coloring ridges. Default is \code{"Phylum"}.}

\item{top_n}{Number of top taxa to display. Default is \code{12}.}

\item{group_by}{Column in \code{sample_data} to color sample points. If \code{NULL},
all points are the same color.}

\item{palette}{A named character vector of colors for \code{color_by}. If \code{NULL},
uses built-in palette.}

\item{scale}{Height scaling of ridges (overlap). Default is \code{2}.}

\item{alpha}{Ridge fill transparency. Default is \code{0.8}.}
}
\value{
A ggplot object.
}
\description{
Draws a ridge (joy) plot showing the abundance distribution of top taxa
across multiple samples. Each ridge represents one taxon, colored by
parent phylum.
}
\examples{
data(demo_data)
plot_ridge(demo_data)
plot_ridge(demo_data, level = "Genus", top_n = 10)
plot_ridge(demo_data, group_by = "Group")
}
