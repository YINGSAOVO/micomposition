% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_radial_bar.R
\name{plot_radial_bar}
\alias{plot_radial_bar}
\title{Radial (circular) stacked bar chart for microbiome composition}
\usage{
plot_radial_bar(
  data,
  level = "Phylum",
  top_n = 8,
  group_by = NULL,
  palette = NULL,
  inner_radius = 0.3
)
}
\arguments{
\item{data}{A list with elements \code{otu_table}, \code{tax_table}, and \code{sample_data},
as in \link{demo_data}.}

\item{level}{Taxonomic level to display. Default is \code{"Phylum"}.}

\item{top_n}{Number of top taxa to show, others merged into "Others".
Default is \code{8}.}

\item{group_by}{Column name in \code{sample_data} to order samples. If \code{NULL},
uses original sample order.}

\item{palette}{A named character vector of colors. If \code{NULL}, uses built-in.}

\item{inner_radius}{Size of the inner hole (0-1). Default is \code{0.3}.}
}
\value{
A ggplot object.
}
\description{
Draws a polar coordinate stacked bar chart where each bar represents a
sample and segments represent taxa. A visually striking alternative to
the standard stacked bar chart.
}
\examples{
data(demo_data)
plot_radial_bar(demo_data)
plot_radial_bar(demo_data, level = "Family", top_n = 10)
plot_radial_bar(demo_data, group_by = "Group")
}
