% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_treemap.R
\name{plot_treemap}
\alias{plot_treemap}
\title{Rectangular treemap for microbiome composition}
\usage{
plot_treemap(
  data,
  sample = NULL,
  level = "Family",
  group_by = "Phylum",
  top_n = 30,
  min_abundance = 0.005,
  palette = NULL,
  label_min_pct = 1,
  show_group_label = TRUE
)
}
\arguments{
\item{data}{A list with elements \code{otu_table}, \code{tax_table}, and \code{sample_data},
as in \link{demo_data}.}

\item{sample}{Name of the sample to display. If \code{NULL}, uses the mean
abundance across all samples.}

\item{level}{Taxonomic level for the inner tiles. Default is \code{"Family"}.}

\item{group_by}{Taxonomic level for grouping and coloring. Default is
\code{"Phylum"}.}

\item{top_n}{Number of top taxa (at \code{level}) to display. Default is \code{30}.}

\item{min_abundance}{Minimum abundance to include a taxon. Default is \code{0.005}.}

\item{palette}{A named character vector of colors. If \code{NULL}, uses built-in.}

\item{label_min_pct}{Minimum percentage to show a tile label. Default \code{1}.}

\item{show_group_label}{Logical. Whether to show group (Phylum) labels.
Default \code{TRUE}.}
}
\value{
A ggplot object.
}
\description{
Draws a rectangular treemap where tile area represents relative abundance.
Tiles are grouped and colored by a parent taxonomic level (e.g. Phylum),
with subdivisions showing a child level (e.g. Family).
}
\examples{
data(demo_data)
plot_treemap(demo_data)
plot_treemap(demo_data, level = "Genus", top_n = 20)
plot_treemap(demo_data, group_by = "Phylum", level = "Family")
}
